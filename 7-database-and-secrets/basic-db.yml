---
integrations:
  - name: nri-flex
    interval: 30s
    config:
      name: pgsqlDbFlex
      apis:
        - database: postgres
          event_type: Lab7Example1
          db_conn: "host=postgres port=5432 user=flexuser password=flexpass dbname=flexworkshop sslmode=disable"
          custom_attributes: # applies to all queries
            weather: sunny
            LOCATION: sydney
        #  db_queries:
        #    - name: userSample
        #      event_type: userSample
        #      run: Select * from users
        #    - name: PGShowVars
        #      event_type: pgsqlvarSample  
        #      run: SELECT * FROM pg_settings WHERE name LIKE 'max%' LIMIT 2;
